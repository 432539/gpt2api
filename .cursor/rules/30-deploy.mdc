---
description: KleinAI 部署 / Docker / Nginx 规范，针对 deploy/ 目录与 Dockerfile 生效。
globs:
  - deploy/**
  - "**/Dockerfile"
  - "**/docker-compose*.yml"
---

# KleinAI · 部署 / 运维规则

> 详见 `docs/06-部署与运维规范.md`

## 端口（对外仅 3 个）

- `17080` → 用户端 Web（HTTPS）
- `17088` → 管理后台 Web（HTTPS，限 IP）
- `17200` → OpenAI 兼容 API（HTTPS）
- 其余端口（17180/17188/13306/16379/17390/17490/17491/17590/17690）**仅内网**或 `127.0.0.1` 监听

## Docker

- 后端：多阶段构建，`distroless/static-debian12:nonroot` 运行
- 前端：multi-stage `node:20-alpine` build → `nginx:1.27-alpine` serve
- 镜像必须签名（cosign）；生产仅拉签名镜像
- 容器以 **非 root** 用户运行
- CPU / 内存 limits 必填

## Compose

- 文件：`deploy/docker-compose.yml`（基础）+ `docker-compose.prod.yml`（生产覆盖）
- env 文件：`deploy/env/.env.prod`，**绝不**入仓
- MySQL / Redis 必须 **仅 127.0.0.1 暴露**

## Nginx

- TLS 1.2 / 1.3，HSTS 1 年
- 必加响应头：`X-Frame-Options SAMEORIGIN` / `X-Content-Type-Options nosniff` / `Referrer-Policy strict-origin-when-cross-origin`
- WebSocket 路径 `/ws` 必须 `Upgrade` 头转发
- OpenAI 兼容路径 `proxy_read_timeout 600s`（视频生成长任务）
- 管理后台必须 `allow / deny` 限 IP 段或前置 basic-auth

## 监控

- Prometheus 抓 `/metrics`，仅内网或带 IP 白名单
- Grafana 看板基线：HTTP / 任务 / 账号池 / 计费四张
- 告警渠道：企业微信机器人；P1 加短信

## 备份

- MySQL：每日 xtrabackup → S3，binlog 归档 7 天
- Redis：AOF + RDB 双开
- 文件：对象存储跨区域复制 + 6 个月转归档

## CI / CD

- GitHub Actions / GitLab CI 流程：lint → test → build → push → deploy
- dev / staging 自动；prod 必须 PR 审批
- 数据库迁移用 `goose / atlas`，**向前兼容**：先发 schema 再发代码

## 不允许

1. 不允许在 docker-compose 直接写明文密码 / 密钥（必须走 env）
2. 不允许 22 / 80 / 443 / 3306 / 6379 / 8080 等常用端口对外
3. 不允许容器以 root 用户启动
4. 不允许把 `.env*` / 证书 / 密钥提交到仓库
