import { PlaceholderPage } from '../_placeholder';

export default function UsersPage() {
  return (
    <PlaceholderPage
      title="用户管理"
      desc="检索 / 充值 / 调整点数 / 封禁，支持按手机号、邮箱、UID、IP、注册时间筛选。"
    />
  );
}
