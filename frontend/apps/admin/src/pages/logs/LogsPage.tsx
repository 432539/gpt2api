import { PlaceholderPage } from '../_placeholder';

export default function LogsPage() {
  return (
    <PlaceholderPage
      title="请求日志"
      desc="按 trace_id / 用户 / 路径 / 状态码搜索，最近 30 天可查。"
    />
  );
}
